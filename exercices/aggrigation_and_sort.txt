1:SELECT productVendor,COUNT(*) as total_vendor FROM `products` GROUP BY productVendor;

2: SELECT status,COUNT(*) FROM `orders` GROUP BY status;

3: SELECT salesRepEmployeeNumber,AVG(creditLimit)
FROM `customers`
GROUP BY salesRepEmployeeNumber;

4:SELECT country,MIN(creditLimit),MAX(creditLimit) FROM `customers` GROUP BY country;

5:SELECT officeCode,COUNT(*) FROM `employees` GROUP BY officeCode;

6:SELECT customerNumber, AVG(amount) as average_payments
FROM `payments`
GROUP BY customerNumber
ORDER BY average_payments DESC;

7:SELECT productVendor,AVG(buyPrice) FROM `products`
GROUP BY productVendor;

8:SELECT salesRepEmployeeNumber,COUNT(*)
FROM `customers`
GROUP BY salesRepEmployeeNumber;

9:SELECT orderDate,COUNT(*) AS orders_date
FROM `orders` 
GROUP BY orderDate
ORDER BY orders_date DESC

10:SELECT productScale,COUNT(*)
FROM `products` 
GROUP BY productScale;